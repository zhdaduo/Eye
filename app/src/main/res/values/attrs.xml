<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="SuperEditText">

        <attr name="ic_delete" format="reference" />
        <attr name="delete_x" format="integer" />
        <attr name="delete_y" format="integer" />
        <attr name="delete_width" format="integer" />
        <attr name="delete_height" format="integer" />

        <attr name="ic_left_click" format="reference" />
        <attr name="ic_left_unclick" format="reference" />
        <attr name="left_x" format="integer" />
        <attr name="left_y" format="integer" />
        <attr name="left_width" format="integer" />
        <attr name="left_height" format="integer" />

        <attr name="lineColor_click" format="color" />
        <attr name="lineColor_unclick" format="color" />
        <attr name="linePosition" format="integer" />

        <attr name="cursor" format="reference" />

    </declare-styleable>

    <declare-styleable name="ShadowLayout">
        <attr name="shadow_layout_color" format="color" />
        <attr name="shadow_layout_background_color" format="color" />
        <attr name="shadow_layout_offsetX" format="dimension" />
        <attr name="shadow_layout_offsetY" format="dimension" />
        <attr name="shadow_layout_blur" format="dimension" />
        <attr name="shadow_layout_radius" format="dimension" />
    </declare-styleable>

    <declare-styleable name="FlingLayout">
        <attr name="fl_canDrag" format="boolean" />
        <attr name="fl_canDismiss" format="boolean" />
    </declare-styleable>

    <declare-styleable name="ImageNice9Layout">
        <attr name="nice9_itemMargin" format="dimension" />
    </declare-styleable>

    <declare-styleable name="AndroidTagGroup">
        <!-- Whether the tag group is in append mode. -->
        <attr name="atg_isAppendMode" format="boolean" />
        <attr name="atg_isRoundMode" format="boolean" />
        <!-- If the tag group is in append mode, what the hint of input tag. -->
        <attr name="atg_inputHint" format="string" />

        <!-- The tag view outline border color. -->
        <attr name="atg_borderColor" format="color" />
        <!-- The tag view text color. -->
        <attr name="atg_textColor" format="color" />
        <!-- The tag view font assets path. -->
        <attr name="atg_textTypefaceAssetPath" format="string" />

        <!-- The tag view background color. -->
        <attr name="atg_backgroundColor" format="color" />

        <!-- The dash outline border color, when in append mode. -->
        <attr name="atg_dashBorderColor" format="color" />
        <!-- The input tag hint text color, when in append mode. -->
        <attr name="atg_inputHintColor" format="color" />
        <!-- The input tag type text color, when in append mode. -->
        <attr name="atg_inputTextColor" format="color" />
        <!-- The input tag type background color, when in append mode. -->
        <attr name="atg_inputTagBackgroundColor" format="color" />

        <!-- The checked tag view outline border color. -->
        <attr name="atg_checkedBorderColor" format="color" />
        <!-- The checked text color. -->
        <attr name="atg_checkedTextColor" format="color" />
        <!-- The checked marker color. -->
        <attr name="atg_checkedMarkerColor" format="color" />
        <!-- The checked tag view background color. -->
        <attr name="atg_checkedBackgroundColor" format="color" />

        <!-- The tag view background color, when the tag view is being pressed. -->
        <attr name="atg_pressedBackgroundColor" format="color" />

        <!-- The tag view outline border stroke width. -->
        <attr name="atg_borderStrokeWidth" format="dimension" />
        <!-- The tag view text size. -->
        <attr name="atg_textSize" format="dimension" />
        <!-- The input tag view text size in append mode. -->
        <attr name="atg_inputTextSize" format="dimension" />

        <attr name="atg_horizontalSpacing" format="dimension" />
        <attr name="atg_verticalSpacing" format="dimension" />
        <attr name="atg_horizontalPadding" format="dimension" />
        <attr name="atg_verticalPadding" format="dimension" />

        <attr name="atg_xRadius" format="dimension" />
        <attr name="atg_yRadius" format="dimension" />

        <attr name="atg_tagsLimitation" format="integer" />
        <attr name="atg_charsLimitation" format="integer" />
    </declare-styleable>

    <declare-styleable name="Themes">
        <attr name="tagGroupStyle" format="reference" />
    </declare-styleable>
</resources>